(->TodoItem "Hello, world!")

:select
[Event (= ?data data) (= ?options options)]
:execute
(js/console.log "Event" (pr-str ?data))

:select
[?items <- (all) :from [TodoItem]]
:insert
(->View "#app"
  [:div
   [:input {:on-key-down {:id :input}}]
   [:button {:on-click {:id :btn}}
    "Add item"]
   (into [:ul]
     (for [item ?items]
       [:li (:text item)
        [:button {:on-click item} "Remove item"]]))])

